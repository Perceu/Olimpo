services:
  olimpo:
    build: 
      dockerfile: php.dockerfile
    ports:
      - 8080:80
    volumes:
      - ./:/var/www/html/Olimpo

  db:
    image: mariadb:latest
    environment:
      MARIADB_ROOT_PASSWORD: senhafraca123
      MARIADB_DATABASE: Infox
    ports:
      - 3306:3306
    volumes:
      - ./db:/docker-entrypoint-initdb.d
